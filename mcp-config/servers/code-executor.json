{
  "name": "code-executor",
  "type": "SSE",
  "url": "http://mcp-code-executor.mcp-executor.svc.cluster.local:80/mcp",
  "description": "Python code execution sandbox with pre-configured Portainer API client (token-efficient: ~200 tokens vs 10,000+)",
  "enabled": true,
  "metadata": {
    "service": "mcp-code-executor",
    "namespace": "mcp-executor",
    "port": 80,
    "added": "2025-12-23T00:00:00Z",
    "tools": [
      "execute_python"
    ],
    "features": [
      "Token-efficient code execution",
      "Pre-authenticated Portainer client",
      "Python sandbox isolation",
      "Automatic result summarization"
    ],
    "usage_example": "import portainer_client as pc; containers = pc.list_containers(endpoint_id=1); print(len(containers))"
  }
}
