apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-home
  namespace: claude
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
