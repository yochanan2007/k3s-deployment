apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: authentik
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Uses K3s local-path-provisioner for dynamic provisioning
---
