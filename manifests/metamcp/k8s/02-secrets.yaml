apiVersion: v1
kind: Secret
metadata:
  name: metamcp-secrets
  namespace: metamcp
  labels:
    app.kubernetes.io/name: metamcp
    app.kubernetes.io/component: config
type: Opaque
stringData:
  # PostgreSQL Database Configuration
  POSTGRES_USER: "metamcp_user"
  POSTGRES_PASSWORD: "clSzPQzXjj5vuL0scuffsaLe7bL8dZg1"
  POSTGRES_DB: "metamcp_db"

  # Database Connection URL
  DATABASE_URL: "postgresql://metamcp_user:clSzPQzXjj5vuL0scuffsaLe7bL8dZg1@metamcp-postgres:5432/metamcp_db"

  # Better Auth Secret
  # Generated with: openssl rand -base64 32
  BETTER_AUTH_SECRET: "75VFEUPdpxnwpq+DO+C+QWa6PFSXgE5uv2omoXVCGys="

  # OIDC Configuration (Optional - for future use)
  # OIDC_CLIENT_ID: ""
  # OIDC_CLIENT_SECRET: ""
  # OIDC_DISCOVERY_URL: "https://auth.k3s.dahan.house/application/o/<app-slug>/.well-known/openid-configuration"
  # OIDC_AUTHORIZATION_URL: "https://auth.k3s.dahan.house/application/o/authorize/"
  # OIDC_PROVIDER_ID: "authentik"
  # OIDC_SCOPES: "openid email profile"
  # OIDC_PKCE: "true"
