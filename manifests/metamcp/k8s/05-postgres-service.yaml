apiVersion: v1
kind: Service
metadata:
  name: metamcp-postgres
  namespace: metamcp
  labels:
    app.kubernetes.io/name: metamcp
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    app.kubernetes.io/name: metamcp
    app.kubernetes.io/component: database
