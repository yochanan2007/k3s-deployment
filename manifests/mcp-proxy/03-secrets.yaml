apiVersion: v1
kind: Secret
metadata:
  name: mcp-proxy-api-tokens
  namespace: mcp-proxy
type: Opaque
stringData:
  # Base64 encode your API tokens before deploying
  # Leave empty if not using these services
  AUTHENTIK_API_TOKEN: ""
  RANCHER_API_TOKEN: ""
---
