apiVersion: v1
kind: Service
metadata:
  name: rustdesk-webclient
  namespace: rustdesk
  labels:
    app: rustdesk-webclient
spec:
  type: ClusterIP
  selector:
    app: rustdesk-webclient
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
