apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-home-workspace
  namespace: claude-code-home
  labels:
    app.kubernetes.io/name: claude-code-home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-path
